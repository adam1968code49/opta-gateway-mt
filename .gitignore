# credentials -- never commit. See secrets.h.example and the AWH_Bridge.exe
# incident in the parent repo for why this line exists before the files do.
secrets.h
arduino_secrets.h

# generated by tools/build.sh from git describe
version.h

# arduino-cli outputs if anyone builds in-tree
build/
*.bin
*.elf
*.hex
*.map
